Listing images for the website and the demo recording, not for the plugin package: the
portal accepts screenshots only when the MCP server returns UI (`screenshots_not_allowed`),
and this one returns none.

- rsvp-page.png: `demo-sites/garden-party-rsvp/index.html` as published.
- survey-results.png, shop-page.png: `demo-sites/feedback-survey/results.html` and
  `demo-sites/pickle-shop/index.html` rendered with the `seed.json` sample data.
- your-sites.png: the reviewer demo account's "Your sites" page after
  `scripts/seed-reviewer-demo.py`, on a local instance (the showcase link reads localhost).

All names and data are fictional sample data.
